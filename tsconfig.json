// note: In vite, this tsconfig is for the ide

{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    // Path aliases
    "paths": { "@/*": ["./src/*"] }
  }
}
